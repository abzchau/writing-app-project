const nameSubmitPhoto = document.getElementById('name_photo');

const descSubmitPhoto = document.getElementById('desc_photo');

const btnSubmitPhoto = document.getElementById('mySubmitPhotoBtn');



function submitPhoto(resSubmitPhoto) {
    let nameSubmitPhoto = document.getElementById('name_photo').value 
    let descSubmitPhoto = document.getElementById('desc_photo').value
}


btnSubmitPhoto.addEventListener('click', submitPhoto)